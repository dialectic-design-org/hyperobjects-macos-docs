# [🏗️] enum: GeometryType

:::info

Initial implementation of `GeometryType` present for `.line` to create data flows, to be extended with further types.

:::

Defines the types of geometries that can be generated as an enumeration.

Current types:

```swift
.line
```


```swift
.bezierCurve
```